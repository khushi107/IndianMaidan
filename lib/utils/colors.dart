import 'package:flutter/material.dart';

class AppColors {
  static const saffron = Color(0xFFFF6B35);
  static const white   = Color(0xFFFFFFFF);
  static const navy    = Color(0xFF1E40AF);
  static const green   = Color(0xFF059669);

  // bg gradient from white → light blues per UI
  static const bgTop    = Color(0xFFFFFFFF);
  static const bgMid    = Color(0xFFF8FAFC);
  static const bgBottom = Color(0xFFF1F5F9);
}
